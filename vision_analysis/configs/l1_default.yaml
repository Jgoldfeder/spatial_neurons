# L1 regularization study config
name: l1
mode: l1

# Hyperparameter search ranges
gamma_min: 1
gamma_max: 500000
log_scale: true

# Training settings
trials: 100
epochs: 10
lr: 0.0001
batch_size: 128
sparsity: 90
dataset: cifar100
model: vit_tiny_patch16_224
